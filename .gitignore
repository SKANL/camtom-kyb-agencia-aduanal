# Dependencies
node_modules/
.pnp
.pnp.js

/email-draft.md
no-commitear/

# shadcn MCP tooling artifacts en la raíz (el config real es .mcp.json; estos son
# regenerables vía `npx shadcn@latest mcp`). Solo la raíz — el frontend/ tiene el suyo.
/package.json
/package-lock.json

# Cachés/registro de skills de Claude Code (Agent Teams Lite / superpowers) — locales
# a la herramienta, regenerables, no son contenido del proyecto KYB.
/.agents/
/.atl/
/.claude/
/skills-lock.json

# Python / uv
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Env / secrets — NUNCA commitear
.env
.env.*
!.env.example

# Build outputs
.next/
out/
dist/
build/

# Vercel
.vercel/

# Supabase local
.branches/
.temp/

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
.vercel
